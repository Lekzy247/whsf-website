name: AgriLearn AI Workforce Suite (manual only)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow paused
        run: echo "AgriLearn AI workforce automation is paused."
