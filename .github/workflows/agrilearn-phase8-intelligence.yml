name: AgriLearn Phase 8 Intelligence (manual only)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow paused
        run: echo "Phase 8 intelligence automation is paused."
