name: Integrate AgriLearn on homepage (manual only)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow paused
        run: echo "Automatic homepage integration is paused. Run manually only after review."
