name: AgriLearn Phase 7 Secure Staging (manual only)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow paused
        run: echo "Phase 7 staging automation is paused."
