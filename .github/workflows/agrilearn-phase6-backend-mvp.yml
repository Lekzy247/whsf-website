name: AgriLearn Phase 6 Backend MVP (manual only)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow paused
        run: echo "Phase 6 code-generation automation is paused."
