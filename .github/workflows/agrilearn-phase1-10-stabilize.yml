name: Stabilize AgriLearn Phases 1-10 (manual only)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow paused
        run: echo "AgriLearn stabilization automation is paused."
