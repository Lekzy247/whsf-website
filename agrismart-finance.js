(()=>{'use strict';const K='agrismart-finance-v1';const id=p=>p+'-'+Date.now().toString(36)+Math.random().toString(36).slice(2,6);const init=()=>({accounts:[],journals:[],budgets:[]});const load=()=>{try{return {...init(),...JSON.parse(localStorage.getItem(K)||'{}')}}catch{return init()}};const save=s=>{localStorage.setItem(K,JSON.stringify(s));return s};function addAccount(a){const s=load();const x={id:id('acct'),code:String(a.code||''),name:String(a.name||''),type:String(a.type||'Asset')};if(!x.code||!x.name)throw Error('Code and name required');s.accounts.push(x);save(s);return x;}function postJournal(j){const s=load();const e={id:id('jrnl'),date:j.date||new Date().toISOString().slice(0,10),description:String(j.description||''),debit:Number(j.debit||0),credit:Number(j.credit||0)};s.journals.unshift(e);save(s);return e;}function addBudget(b){const s=load();const x={id:id('bud'),name:String(b.name||''),amount:Number(b.amount||0),period:String(b.period||'Annual')};s.budgets.push(x);save(s);return x;}function dashboard(){const s=load();return{accounts:s.accounts.length,journals:s.journals.length,budgets:s.budgets.length,totalBudget:s.budgets.reduce((t,b)=>t+b.amount,0)}}window.AgriSmartFinance=Object.freeze({load,addAccount,postJournal,addBudget,dashboard});})();